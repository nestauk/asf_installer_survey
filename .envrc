#!/bin/bash
[ -f .env ] && source .env

# export S3_INPUT_PATH="s3://nesta-ds-projects/<MISSION OR PRACTICE>/<PROJECT NAME>"

# Activate the conda environment
eval "$(conda shell.bash activate "asf_installer_survey")"
[ "$CONDA_DEFAULT_ENV" = "asf_installer_survey" ] || echo "Have you run \`make install\`?"
